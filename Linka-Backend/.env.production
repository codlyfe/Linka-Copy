# LinkA Backend Production Environment Configuration
# This file contains production-specific configuration for the backend

# Application Configuration
APP_NAME=linka-backend
APP_VERSION=1.0.0
SERVER_PORT=8081
PROFILE=prod

# Database Configuration (Production PostgreSQL)
DB_HOST=postgres.linka.internal
DB_PORT=5432
DB_NAME=linka_prod
DB_USERNAME=linka_prod_user
DB_PASSWORD=G7!vR9q$2zX@Lm#4pT8wYk^1sB
DB_URL=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=require

# JWT Configuration (Production Security)
# IMPORTANT: Generate a strong 256-bit (64 character) secret for production
# Use: openssl rand -hex 32  (Linux/Mac) or PowerShell: [System.BitConverter]::ToString((1..32 | % {Get-Random -Maximum 256})).Replace("-", "")
JWT_SECRET=${JWT_SECRET}  # Set this as an environment variable in production
JWT_EXPIRATION=86400000

# Email Configuration (Production SMTP)
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_USERNAME=noreply@linka.com
SMTP_PASSWORD=your_email_password
FROM_EMAIL=noreply@linka.com
EMAIL_ENABLED=true

# Mobile Money Configuration
MOBILE_MONEY_MTN_MIN=500
MOBILE_MONEY_MTN_MAX=5000000
MOBILE_MONEY_MTN_FEE=0.01
MOBILE_MONEY_AIRTEL_MIN=500
MOBILE_MONEY_AIRTEL_MAX=3000000
MOBILE_MONEY_AIRTEL_FEE=0.015
MOBILE_MONEY_MULA_MIN=1000
MOBILE_MONEY_MULA_MAX=2000000
MOBILE_MONEY_MULA_FEE=0.005

# Security Configuration
BCRYPT_ROUNDS=12
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CSRF_ENABLED=true

# Logging Configuration (Production optimized)
LOG_LEVEL=WARN
LOG_FILE_PATH=/var/log/linka/application.log
JSON_LOGGING=true

# File Upload Configuration
UPLOAD_PATH=/app/uploads/
MAX_FILE_SIZE=10MB
ALLOWED_IMAGE_TYPES=jpg,jpeg,png,webp

# Health Check Configuration
HEALTH_CHECK_ENABLED=true

# Production Settings
DEBUG_MODE=false
SPRING_DEVTOOLS_ENABLED=false
ACTUATOR_ENABLED=true

# Optional: Redis Configuration (uncomment if using Redis)
# REDIS_HOST=redis.linka.internal
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password
# REDIS_DATABASE=0

# Optional: Monitoring Configuration (uncomment if needed)
# PROMETHEUS_ENABLED=true
# METRICS_ENABLED=true